# Gradle settings
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric versions (loom for all versions; loom 1.17.x ships both plugin IDs)
loom_version=1.17.18

# Mod properties
mod_version=1.1.0
maven_group=xwvi
archives_base_name=lodestonemod

# Target Minecraft version (see gradle/versions/ for all supported versions)
mc_version=26.2
